import { multiply } from '../../index';

test(
    'multiply on 8 equal 16', () => {
        expect(multiply(8)).toBe(16);
    }
)

test(
    'multiply on 8 equal 16', () => {
        expect(multiply(2)).toBe(16);
    }
)

